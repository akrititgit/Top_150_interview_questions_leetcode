def searchRange(self, nums: List[int], target: int) -> List[int]:
        l1=[]
        l2=[-1,-1]
        if target not in nums:
            return l2
        for i in range(len(nums)):
            if target==nums[i]:
                l1.append(i)
        return l1[0],l1[-1]
