def removeDuplicates(self, nums: List[int]) -> int:
        l=1
        for r in range(1,len(nums)):
            if nums[r]!=nums[r-1]:
                nums[l]=nums[r]
                l+=1
        return l
        '''i=0
        j=1
        while(i<j and j<len(nums)):
            if nums[i]==nums[j]:
                nums.remove(nums[j])
        return len(nums)'''
